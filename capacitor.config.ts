import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.vigilantalpacaclimb',
  appName: 'vigilant-alpaca-climb',
  webDir: 'dist'
};

export default config;
